# Product / features

- Implement card collecting system / trading cards (DB tables `user_cards` + `card_trades` exist; no bot logic wired up yet)
- Spawn random card at certain level ups (not hooked to `libs/cardSystem.js` / XP flow today)
- **Inventory / trading:** implement `giveCard` in [`libs/cardSystem.js`](libs/cardSystem.js) (currently empty stub), insert into `user_cards` using `users.id` (resolve Discord user → `users` row), and add commands or flows for `card_trades` (pending/accept/reject)

# TCG / card game — fixes & follow-ups (code review)

- **[Critical]** [`libs/cardSystem.js`](libs/cardSystem.js): `getRarityChances` calls `end()` after reading `rarity` table — that **destroys the global Knex pool** (same class of bug as channel stats). Remove `end()` from this path.
- **[Bug]** [`src/bot/commands/slashCommands/cards/list_all_cards.js`](src/bot/commands/slashCommands/cards/list_all_cards.js): **total count / `totalPages` ignores the search string** while `fetchCards` applies it — pagination and “Next” are wrong when searching.
- **[Config]** [`list_all_cards.js`](src/bot/commands/slashCommands/cards/list_all_cards.js) requires `client.config.emojis` (`type`, `level`, `power`, `class`, `star`). If any are missing, the command throws — document in config / env or add safe fallbacks.
- **[Bugs / quality]** [`create_card.js`](create_card.js): add missing `silly-logger` import (or remove `logger` usage); declare `starCount` with `let` (avoids implicit global); ensure [`config.cardUrl`](create_card.js) is set in `.config` or generated cards get broken `image_url`s.
- **[Pipeline]** [`master.js`](master.js) imports uploader, mod, staff, and retired JSON but only runs `runBatches(modData)` — other role JSON is generated by [`import_from_discord.js`](import_from_discord.js) but **not** fed through the batch image worker. Confirm intent; add batches or document “mods only.”
- **[Pipeline]** [`batch_worker.js`](batch_worker.js): main try/catch is commented out — failures can exit unclean; restore error handling and non-zero exit on failure.
- **[Pipeline]** [`import_from_discord.js`](import_from_discord.js): retired role ID is **hardcoded**; move to config with other role IDs.
- **[Performance]** Large `card_data` table: add **indexes** (or compounding strategy) for `list_all_cards` filters (`name`, `class`, `rarity`, `created_at`) if queries get slow.
- **[Polish]** Card renderer font names: registered family vs `fillText` font strings in [`create_card.js`](create_card.js) may not match (verify text renders with intended faces).

# Refactor & tooling (order)

1. **Keep flat fixes in place first** — do correctness and small targeted fixes before large restructuring.

2. **Optional later:** split [`database/db.js`](database/db.js) (≈300 lines of mixed concerns) into modules: `repositories/xp.js`, `repositories/moderation.js`, `repositories/cards.js`, and keep `db.js` as the Knex instance + re-exports.

3. **NPM scripts** — add `import:discord` and `tcg:batch` (or document `node master.js` for the TCG batch pipeline) so entry points are discoverable and not from memory.

# Code layout (post flat-fixes)

Do **not** do this before correctness fixes; order below is **suggested effort / payoff**.

1. **[DB]** Split [`database/db.js`](database/db.js) into repositories (`xp`, `moderation`, `cards`, …); keep one Knex instance + re-exports (same as item 2 in Refactor & tooling).

2. **[Bot — message event]** Decompose [`src/bot/events/messages/message.js`](src/bot/events/messages/message.js): separate modules for XP, AFK, channel stats, custom commands, prefix — thin `message.js` that calls them in order (easier to test and to reason about under load).

3. **[Root scripts]** Move TCG / one-off scripts into something like `scripts/tcg/` (`import_from_discord`, `master`, `batch_worker`, `create_card`) so app entrypoints at repo root stay obvious (pairs with npm scripts above).

4. **[Bot — ready]** Split [`src/bot/events/ready/ready.js`](src/bot/events/ready/ready.js): e.g. slash registration vs guild DB sync vs warming `client.guild*prefix` / config caches (smaller functions or files).

5. **[Bot — lifecycle]** Extract scheduled cage job (and similar) from [`src/bot/bot.js`](src/bot/bot.js) into e.g. `src/bot/jobs/cageExpiry.js` so `bot.js` stays wire-up + login + shutdown.

6. **`libs/` vs bot** — Either move [`libs/`](libs/) under `src/shared/` (or `src/lib/`) with domain folders, or keep `libs/` but group by domain (`xp`, `cards`, …) so it does not become a junk drawer. Rule of thumb: bot = Discord shell; lib = business rules + DB use.

7. **[Web, optional]** If route files grow fat with Knex, add a thin `src/web/services/` layer per domain.

8. **[Slash commands, optional later]** Many near-identical “fun” commands — consider a data-driven or generator approach when editing 40+ files gets painful; not a runtime priority.

9. **Avoid for now** — deep DDD / many micro-packages for a single-guild bot; big renames of `src/bot` unless you are already changing those areas (e.g. d.js 14, env).
